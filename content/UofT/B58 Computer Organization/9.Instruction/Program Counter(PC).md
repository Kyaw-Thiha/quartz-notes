# Program Counter
The [[Program Counter(PC)|program counter(PC)]] is a special register that stores the location of the current instruction.

Each instruction is $4$ bytes long, thus we do $+4$ to increment the current [[Program Counter(PC)|PC]] location.

---
