# Greedy Algorithm
A [[Greedy Algorithm|greedy algorithm]] builds a solution in stages. In each stage, it extends a partial solution to a more complete one(in what seems to be the best for the next step[greedily & irrevocably]).

---
