## Graph Representation
Transform raw event streams $\text{(within time window)}$ 
into `graph structure` $\text{(connected nodes)}$ to explicitly preserve and exploit event **sparsity**

### Graph Construction
- **Nodes**: Events or event clusters
- **Edges**: Spatiotemporal connectivity 
  (events close in space and time)

---