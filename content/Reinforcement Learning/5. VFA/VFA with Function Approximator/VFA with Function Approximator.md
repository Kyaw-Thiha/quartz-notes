Let's consider some general approaches for [[Value Function|value function]] computation when we only use functions from a [[Value Function|value function space]] $\mathcal{F}$.

Most of these approaches based on defining a [[Loss Function|loss function]] that should be minimized to find an approximate [[Value Function|value function]].


