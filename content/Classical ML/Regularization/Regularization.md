# Regularization

`Regularization` is a way to limit the model from being over complex and `overfitting`.

`Hard Constraint`: Restricting family of models (eg only using small degree polynomials)
`Soft Constraint`: Encourage smoothness with penalty function
